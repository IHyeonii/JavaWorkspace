package chap_01;

public class _05_VariableNaming { // 변수 이름을 짓자
  public static void main(String[] args) {
    // 1. 저장할 값에 어울리는 이름
    // 2. 밑줄, 문자, 숫자 사용 가능 (공백 불가)
    // 3. 밑줄 또는 문자로 시작 (숫자로 시작 불가)
    // 4. 소문자로 시작
    // 5. 예약어 사용 불가 : public, static 등

    // 지금까지 배운 변수 = 값을 초기화 하고 값을 변경 가능

    // 절대 변하지 않는 상수 = 대문자 (한 번 정의되면 값을 바꿀 수 없다.)

  }
}
